# vo 层结构说明

## 1. 目录定位

`vo` 为接口出参层，定义对外响应数据结构。

## 2. 子目录说明

- `book`：图书接口出参 VO。
- `user`：用户接口出参 VO。

## 3. 链路涉及

service 完成业务处理后，将 entity 转为 VO，再由 controller 返回给客户端。

## 4. 约束

- VO 只表达展示字段，不承载业务行为。

