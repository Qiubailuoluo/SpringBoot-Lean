# dto 层结构说明

## 1. 目录定位

`dto` 为接口入参层，承载请求参数并进行参数校验。

## 2. 子目录说明

- `book`：图书接口入参 DTO。
- `user`：用户接口入参 DTO。

## 3. 链路涉及

controller 接收 DTO，校验通过后交给 service 处理。

## 4. 约束

- DTO 不应包含数据库访问逻辑。
- DTO 与 entity 解耦，不直接复用 entity 作为请求参数。

