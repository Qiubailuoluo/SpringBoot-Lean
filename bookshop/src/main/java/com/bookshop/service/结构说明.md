# service 层结构说明

## 1. 目录定位

`service` 为业务编排层，负责业务规则、对象转换和异常抛出。

## 2. 子目录说明

- `book`：图书业务接口与实现。
- `user`：用户业务接口与实现。

## 3. 链路涉及

controller 调用 service；service 调用 mapper；异常通过 `BusinessException` 抛出并由全局异常处理器收口。

## 4. 约束

- 不直接处理 HTTP 细节。
- 业务异常必须统一抛 `BusinessException`。

