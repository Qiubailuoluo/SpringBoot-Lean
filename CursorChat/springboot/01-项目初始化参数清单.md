# Spring Boot 项目初始化参数清单

## 1. 版本选型（当前已确认）

- JDK：`17`（LTS，生态稳定）
- Spring Boot：`3.5.13`
- 构建工具：`Maven`
- 数据库：`MySQL 8.x`（若低版本需使用兼容排序规则）
- 持久层：`MyBatis`（当前项目已选择，不使用 JPA）

## 2. 本地环境参数（已记录）

- `JAVA_HOME`：`D:/My_download/JDK17`
- `MAVEN_HOME`：`D:/My_download/java_MAVEN/apache-maven-3.6.3`
- Maven 版本：`3.6.3`
- Maven settings：`D:/My_download/java_MAVEN/apache-maven-3.6.3/conf/settings.xml`
- Maven 本地仓库：`D:/My_download/java_MAVEN/apache-maven-3.6.3/repo`
- MySQL 连接：
  - host: `127.0.0.1`
  - port: `3306`
  - db: `bookshop`
  - user: `bookshop_dev`

## 3. Spring Initializr 创建参数

- Project：`Maven`
- Language：`Java`
- Spring Boot：`3.5.13`
- Group：`com.bookshop`
- Artifact：`bookshop`
- Name：`bookshop`
- Packaging：`Jar`
- Java：`17`

## 4. 第一批依赖（仅保留最小集合）

- `Spring Web`
- `MyBatis Framework`（已选择）
- `MySQL Driver`
- `Validation`
- `Lombok`（可选）

> 说明：`MyBatis-Plus` 不在初始化向导中时，后续通过 `pom.xml` 手动添加即可。

## 5. 初始目录结构建议

```text
src/main/java/com/bookshop
  ├─ controller
  ├─ service
  ├─ repository
  ├─ entity
  └─ dto
```

## 6. 第一阶段验收标准

- 应用可启动
- 成功连接 `bookshop` 数据库
- 提供 `/health` 基础健康检查接口
- 提供一个实体的最小 CRUD（增删改查）

## 6.1 当前进展记录（2026-04-13）

- 项目已创建完成：`bookshop`
- 已添加依赖：`Spring Web`、`MyBatis`、`MySQL Driver`、`Validation`、`Lombok`
- 已完成配置：数据源参数写入 `application.yml`
- 已完成接口：`GET /health`
- 已完成启动校验：应用启动时主动检查数据库连接
- 当前配置已升级为：`application.yml`
- 连库验证结果：`mvn clean test` 通过（数据库连接成功）
- 提示：当前无 Mapper 属于正常现象，后续创建首个业务模块时补齐 `mapper` 接口与 XML/注解 SQL

## 6.2 Cursor 终端运行注意

- 当前系统 Maven 在命令行默认使用的是 JDK 8。
- 运行 Spring Boot 3 项目时需临时指定 JDK 17：

```powershell
$env:JAVA_HOME='D:\My_download\JDK17'
$env:Path="$env:JAVA_HOME\bin;$env:Path"
mvn test
```

## 7. 第二阶段预留

当前阶段不要引入 Redis、MinIO、RabbitMQ。等第一阶段稳定后再逐个接入，避免学习曲线过陡与问题定位困难。

